<!DOCTYPE html>
<html>
  <head>
    <title>EndingNote</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

  </head>

  <body>
		<% if notice %>
  		<p class="alert alert-success"><%= notice %></p>
		<% end %>
		<% if alert %>
  		<p class="alert alert-danger"><%= alert %></p>
		<% end %>

		<ul class="nav">
			<p class="navbar-text pull-right">
			<%= link_to "Home", root_path, :class => 'navbar-link', data: { turbolinks: false } %> |
			<% if user_signed_in? %>
  			<%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> |
				<%= link_to "Game", game_path, :class => 'navbar-link', data: { turbolinks: false }  %> |
  			<%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %> |
			<% else %>
  			<%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link'  %> |
  			<%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %> |
			<% end %>
			<% if user_signed_in? %>
				Logged in as <strong><%= current_user.username %></strong> with a highscore of <strong><%= current_user.highscore %></strong>.
			<% end %>
			</p>
		</ul>
    <%= yield %>
  </body>
</html>
